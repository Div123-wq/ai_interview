TYPE: <class 'list'>
[{'question': 'Given an unsorted array of integers `nums`, return the length of the longest consecutive elements sequence. Your algorithm should run in `O(n)` time.', 'type': 'technical', 'hint': 'Key points to cover: Efficient data structure for lookups (e.g., hash set), optimizing the search to avoid redundant checks for sequence starts, handling duplicate numbers, and analyzing time/space complexity.'}, {'question': 'Design a distributed rate limiter for an API endpoint. The system should allow a maximum of `X` requests per `Y` seconds per user. Discuss the key components, data structures, and algorithms you would consider. What are the trade-offs and potential challenges in a distributed environment?', 'type': 'technical', 'hint': 'Key points to cover: Choice of rate limiting algorithm (e.g., sliding window log, token bucket, leaky bucket), data storage considerations (e.g., Redis, shared memory), handling distributed system challenges (consistency, clock synchronization, eventual consistency), identifying edge cases (bursts, network partitions), and discussing scalability and fault tolerance.'}]